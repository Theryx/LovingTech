services:
  loving-tech:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loving-tech-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    restart: always
